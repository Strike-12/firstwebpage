# Running this project on Replit

This project is a static HTML page. Replit serves `index.html` through the
`Start application` workflow using Python's built-in HTTP server on port 5000.

To run it locally, use:

```bash
python3 -m http.server 5000 --bind 0.0.0.0
```